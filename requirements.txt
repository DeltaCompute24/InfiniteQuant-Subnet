# Requires Python 3.10 — `timelock` ships wheels only for 3.7–3.10; 3.11/3.12
# fail with "No matching distribution found for timelock_wasm_wrapper".
# pinned to the versions proven on the MANTIS stack (mantis VM venv)
bittensor==10.3.0
bittensor-wallet==4.0.1
# bittensor 10.3.0 is required for the typed-netuid finney runtime (10.2.0 crashes
# on metagraph(netuid=89)). It uses async-substrate-interface 2.x + cyscale, NOT
# scalecodec. Pin cyscale==0.3.3 -- the range resolves to 0.5.0 whose scalecodec
# shim is broken. Do NOT add scalecodec: async-substrate-interface 2.x aborts if
# it is present (namespace conflict). Verified clean install, no manual steps.
async-substrate-interface==2.0.2
cyscale==0.3.3
# TIMELOCK — CONSENSUS-CRITICAL, and deliberately NOT a PyPI pin.
# The live fleet seals LF reveals with timelock_wasm_wrapper 0.3.0 (245-byte
# W_time). PyPI's newest is 0.0.2 (261-byte), and the `timelock` binding is not
# on PyPI at all — so the old `timelock==0.0.1.dev0` pin built a validator that
# could not open a single live reveal. It did not crash or warn; LF simply went
# ungradeable and its whole COMP_WEIGHTS share burned. Both wheels are vendored
# (MIT / Apache-2.0, redistributable) so a clean install matches consensus.
# Verify after installing:  python tools/check_timelock.py
# The compiled wheel is cp310 manylinux x86_64. On any other platform, build
# timelock_wasm_wrapper 0.3.0 from https://github.com/ideal-lab5/timelock —
# check_timelock.py will tell you plainly if you are on the wrong format.
./vendor/timelock/timelock_wasm_wrapper-0.3.0-cp310-cp310-manylinux_2_34_x86_64.whl ; sys_platform == "linux" and platform_machine == "x86_64"
./vendor/timelock/timelock-0.0.2.dev0-py3-none-any.whl ; sys_platform == "linux" and platform_machine == "x86_64"
# Off cp310-manylinux-x86_64 the compiled wheel above is unusable, and an
# unmarked path requirement fails the WHOLE install ("not a supported wheel
# on this platform") -- run.sh logs that and continues, so the neuron would
# keep running on stale deps. Fall back to the PyPI legacy so the neuron at
# least starts; tools/check_timelock.py, which the validator calls at
# startup, then says plainly that LF cannot be graded on this box.
# Written as De Morgan, NOT `not (...)`: pip's vendored packaging rejects the
# `not (...)` marker form with InvalidMarker, which aborts the entire install.
timelock==0.0.1.dev0 ; sys_platform != "linux" or platform_machine != "x86_64"
cryptography>=42.0
requests>=2.31
# miner upload path only
boto3>=1.34
# dev
pytest>=8.0
websockets>=12
